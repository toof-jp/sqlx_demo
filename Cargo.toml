[package]
name = "sqlx_demo"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = "0.15.0"
sqlx = { version = "0.7.4", features = [ "postgres", "runtime-tokio-native-tls" ]}
tokio = { version = "1.37.0", features = [ "full" ]}